import { Button } from "@/components/ui/button"
import Image from "next/image"

export default function Home() {
  return (
    <div>
      MAIN PAGE
      <br />
      <Button title="Start" variant={"primary"}>
        Start
      </Button>
      <Button title="Start" variant={"glass"}>
        Start
      </Button>
    </div>
  )
}
